import React from 'react'
import { Text, View } from 'react-native'
import { globalStyles } from '../../theme/theme'

export const SettingsScreen = () => {
    return (
        <View>
            <Text style={globalStyles.componentTitle}>
                SettingsScreen
            </Text>
        </View>
    )
}
